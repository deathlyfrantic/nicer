// @flow
import React, { Component } from "react";

// this class just exists to have a draggable
// section of the electron window
class Titlebar extends Component<{}> {
  render() {
    return <div id="titlebar" />;
  }
}

export default Titlebar;
